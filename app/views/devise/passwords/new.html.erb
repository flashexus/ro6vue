<body class="entry vl-bodyBg">
  <% unless notice.nil? %>
  <p class="notice"><%= notice %></p>
  <%end%>
  <% unless alert.nil? %>
  <p class="alert"><%= alert %></p>
  <%end%>

  <header class="vl-appTop-header py-3">
  </header>
  <div class="container">
    <div class="vl-contentsWrap">
      <div class="vl-contentBlock rounded m-auto bg-white card">
        <div class="card-header">
          <h1 class="text-main-color"><%=  t('.forgot_your_password') %></h1>
        </div>
        <div class="vl-cardWrap">
          <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
            <div class="vr-formWrap">
              <%= bootstrap_devise_error_messages! %>
              <dl class="form-group form-row">
                <dt class="col-sm-3">
                  <%= f.label :email ,class: 'vl-label required' %>
                </dt>
                <dd class="col-sm-9">
                  <div class="vl-input mb-2">
                     <%= f.email_field :email, 
                          autofocus: true, 
                          autocomplete: 'email', 
                          class:'form-control', 
                          required:"required", 
                          placeholder:"例：iwami@example.com" %>
                  </div>
                  <p style="font-size:1.0rem;">携帯電話のメールアドレスを入力される方でパソコンからの受信メールを制限している方は解除をお願いします。
（@vitallead.co.jpのドメインからメールが受信できるよう解除をお願いします。）</p>

               </dd>
              </dl>
              <div class="form-group">
                <%= f.submit t('.send_me_reset_password_instructions'), class: 'btn btn-main-color btn-lg btn-block' %>
              </div>
            </div><!-- / .vr-formWrap -->
          <% end %>
          <%= render 'devise/shared/links' %>
        </div><!-- / .vl-cardWrap -->
      </div><!-- / .vl-enterBlock -->
    </div><!-- / .vl-enterWrap -->
  </div><!-- / .container -->
  <footer class="vl-footer py-3 text-center">
    <small><a href="https://www.all-iwami.com/contents/stamprally/">©</a></small>
  </footer>
</body>